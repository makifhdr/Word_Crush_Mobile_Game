<?xml version="1.0" encoding="utf-8"?>

<toolkit:Popup x:TypeArguments="system:Boolean" xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
               xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
               xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
               xmlns:system="clr-namespace:System;assembly=System.Runtime"
               x:Class="MauiApp5.Views.OnayPopup"
               BackgroundColor="{StaticResource Gray900}">
    <Border WidthRequest="320"
            BackgroundColor="DarkSlateGray"
            Stroke="White"
            StrokeThickness="2"
            Padding="20">
        
        <Border.StrokeShape>
            <RoundRectangle CornerRadius="20" />
        </Border.StrokeShape>

        <VerticalStackLayout Spacing="20">
            <Label x:Name="BaslikLbl"
                   FontSize="22" 
                   FontAttributes="Bold"
                   HorizontalOptions="Center" />
            
            <Label x:Name="MesajLbl"
                   FontSize="16"
                   HorizontalOptions="Center"
                   HorizontalTextAlignment="Center" />
            
            <HorizontalStackLayout HorizontalOptions="Center" Spacing="20">
                <Button Text="Evet" 
                        WidthRequest="100"
                        CornerRadius="10"
                        Clicked="OnEvetClicked" 
                        BackgroundColor="{StaticResource Gray900}"/>
                
                <Button Text="Hayır" 
                        WidthRequest="100"
                        CornerRadius="10"
                        Clicked="OnHayirClicked"
                        BackgroundColor="{StaticResource Gray900}"/>
                
            </HorizontalStackLayout>
        </VerticalStackLayout>
    </Border>
</toolkit:Popup>